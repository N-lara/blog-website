module.exports = {
    ///////////////////TODO format time for use with post date usage module 14 activity 20 middleware
    // format_time: (date) => {
    //   return date.toLocaleTimeString();
    // },
    // format_date: (date) => {
    //   return `${new Date(date).getMonth() + 1}/${new Date(date).getDate()}/${
    //     new Date(date).getFullYear() + 5
    //   }`;
    // },
  };
  